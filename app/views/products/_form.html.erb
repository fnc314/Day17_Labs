<%= form_for @product do |f| %>

<%= f.text_field :name, placeholder: "Product Name" %> <br>
<%= f.number_field :price, step: 0.01, placeholder: "Price XX.XX" %> <br>
<%= collection_check_boxes :product, :category_ids, Category.all, :id, :name %>

<%= f.submit %>

<% end %>