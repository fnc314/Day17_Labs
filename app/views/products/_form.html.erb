<%= form_for @product do |f| %>

<%= f.text_field :name, placeholder: "Product Name" %>
<%= f.number_field :price, placeholder: "Price XX.XX" %>
<%= collection_check_boxes :product, :category_ids, Category.all, :id, :name %>

<%= f.submit %>

<% end %>